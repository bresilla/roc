syntax = "proto3";

message Robot {
    message Status {
        bool is_moving = 1;
    }
    Status robot_status = 1;
}

message Command {
    Robot.Status required_status = 1;
    string command_text = 2;
}
