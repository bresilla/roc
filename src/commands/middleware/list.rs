use clap::ArgMatches;

pub fn handle(_matches: ArgMatches) {
    println!("Middleware list functionality is currently work in progress.");
    println!("This will list available ROS2 middleware implementations.");
}
