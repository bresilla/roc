use clap::ArgMatches;

pub fn handle(_matches: ArgMatches) {
    println!("Middleware get functionality is currently work in progress.");
    println!("This will get the current ROS2 middleware implementation.");
}
