use clap::ArgMatches;

pub fn handle(_matches: ArgMatches) {
    println!("Middleware set functionality is currently work in progress.");
    println!("This will set the ROS2 middleware implementation.");
}
